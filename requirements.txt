## Platelet whole-cell model — Python runtime requirements.
## Python 3.11.5, managed with uv (see docs/environment.md). uv reads the pinned
## interpreter from .python-version.
##
## Set up:
#   uv python install 3.11.5      # installs the pinned interpreter if needed
#   uv venv                       # creates .venv from .python-version
#   uv pip install -r requirements.txt
#
## Run simulations with:
#   OPENBLAS_NUM_THREADS=1 PYTHONPATH="$PWD" uv run python runscripts/manual/runPlateletSim.py
#
## For CI, use requirements-platelet-ci.txt (subset of this file).

# ── Core science ──────────────────────────────────────────────────────────────
numpy==1.26.3
scipy==1.11.4
matplotlib==3.7.1
Unum==4.2.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-cov==4.1.0
mock==5.0.2

# ── Type checking ─────────────────────────────────────────────────────────────
mypy==1.3.0
mypy-extensions==1.0.0
types-mock==5.0.0.6

# ── Web interface (Dash/Flask) ─────────────────────────────────────────────────
dash==2.18.2
plotly==5.24.1

# ── Terminal UI (Textual) ───────────────────────────────────────────────────────
textual==8.2.7
textual-plotext==1.0.1
plotext==5.3.2


ipython==8.13.2
ipdb==0.13.13
line-profiler==4.0.3

# ── Terminal replay TUI (optional) ─────────────────────────────────────────────
# The animated replayer (runscripts/manual/replayTui.py) needs rich + textual.
# Kept out of the core install — see requirements-viz.txt.
