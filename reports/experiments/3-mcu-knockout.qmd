---
title: "MCU knockout: mitochondrial Ca²⁺ uptake"
subtitle: "Platelet whole-cell model — demo experiment 3 of 4"
author: "Steve Haigh"
date: 2026-06-23
format:
  pdf:
    pdf-engine: xelatex
    geometry: "margin=2.5cm"
    fontsize: 11pt
    mainfont: "STIX Two Text"
    colorlinks: true
    linkcolor: blue
    urlcolor: blue
    toc: true
    toc-depth: 2
    include-in-header: ../pandoc-header.tex
  html:
    toc: true
    embed-resources: true
---

## Purpose

Knock out the **mitochondrial calcium uniporter (MCU)** on the
[baseline experiment](1-baseline-resting-agonist.qmd) and examine its effect on
the cytosolic Ca²⁺ transient. This is the worked **model-meets-data** example of
the set. The *buffer-only* treatment of MCU (the v0.5 model) made a clear
prediction that ran **opposite** to two independent MCU-knockout studies; that
diagnostic divergence motivated the **v0.7 mitochondrial coupling** (GitHub issue
#76), and with it in place the model now **reproduces the published direction** —
MCU loss *reduces* the agonist-evoked cytosolic transient. Below shows both: why
the buffer-only picture failed, and what coupling fixed it.

## What is changed from the baseline

Exactly **one model setting** changes from the baseline:

| | Baseline | MCU knockout |
|---|---|---|
| `mcu_vmax_scale` | 1.0 | **0.0** |

Everything else is identical (Dolan agonist at 60 s, +Ca²⁺, 300 s).
`mcu_vmax_scale` (a parameter in `reconstruction/platelet/run_config.py`) scales
the maximal MCU flux, so `0.0` removes mitochondrial Ca²⁺ uptake entirely. The
exact commands are in the [Appendix](#appendix).

## Biological reasoning

The original model represented MCU as a pure **cytosolic Ca²⁺ buffer / sink**:
during activation it carries a share of released Ca²⁺ into the matrix. Viewed
purely as a buffer, the expectation is straightforward — remove the sink and the
buffered species (cytosolic Ca²⁺) should **rise**. Nothing counterintuitive; it
is simply what removing a buffer does.

Real platelet MCU does more. Mitochondrial Ca²⁺ uptake **supports** agonist-evoked
store release (via mitochondria–DTS / MAM coupling) and store-operated Ca²⁺ entry
(bioenergetically), so its loss *lowers* the cytosolic transient. The v0.7 model
adds exactly this: a matrix-Ca²⁺ support factor (#76 Part 2) that gates IP3R
release and SOCE, on top of a bounded matrix (#76 Part 1). With MCU knocked out
the matrix depletes, that support falls, and release + SOCE drop — so cytosolic
Ca²⁺ falls, as observed.

## What happens in the model

With the v0.7 coupling, removing MCU now **lowers** the sustained cytosolic Ca²⁺
(WT ~530 → KO ~480 nM; @fig-mcu), and the DTS store **releases less** (the KO
store stays at ~70 µM where the wild type empties to ~2 µM) — the reduced
agonist-evoked-release phenotype. The matrix depletes without MCU, so the support
factor falls and both store release and SOCE are throttled. Integrin output is
essentially unchanged (PAC-1⁺ ~0.82 either way; the readout is saturated under
this strong agonist).

This reverses the *buffer-only* prediction: set `mito_coupling_gain = 0` (decouple
Part 2) and the model reverts to the old behaviour — MCU loss *raises* cytosolic
Ca²⁺ (~+14 % with the #76 Part 1 capacity bound in place; ~+40 % before it). The
coupling is what flips the sign, and the flip is **release-driven** (gating SOCE
alone is too weak — the extrusion pumps compensate as cytosolic Ca²⁺ falls).

![**MCU knockout (current, coloured) vs wild-type baseline (grey) — v0.7 model with the #76 coupling.** Left: removing mitochondrial Ca²⁺ uptake now *lowers* the sustained cytosolic Ca²⁺ (KO ~480 vs WT ~530 nM) — the curve sits *below* the grey baseline, matching the platelet MCU-knockout data. Centre: the KO DTS store releases less and stays fuller (it does not empty to the wild-type ~2 µM) — the reduced-release phenotype; store-operated entry is also throttled. Right: IP₃ is essentially unchanged (the perturbation is downstream of IP₃). Reproduce the old buffer-only divergence (KO *above* WT) with `mito_coupling_gain = 0`. The baseline's duration is shown in its legend label.](../figures/demos/mcu_calcium.png){#fig-mcu width=100%}

## What happens in real platelets

Two independent platelet MCU-knockout studies — **Ajanel et al., *Circ. Res.*
2025; 137(4):474–492** (platelet-specific Mcu^plt−/−^ mice) and **Ghatge et al.,
*J. Thromb. Haemost.* 2026; 24:716–731** (global Mcu⁻/⁻ mice) — found the
**opposite** direction for cytosolic Ca²⁺, and agree with each other:

- **Cytosolic Ca²⁺ was reduced, not raised — in both studies.** Ajanel: agonist-
  evoked Ca²⁺ store release *and* store-operated Ca²⁺ entry were both
  *significantly reduced*, "independent of agonist," including the GPCR agonists
  thrombin and the ADP analogue 2-MeSADP that this experiment uses. Ghatge
  (summarised as "diminished cytosolic Ca²⁺"): agonist-evoked cytosolic Ca²⁺ was
  significantly attenuated under both the GPVI agonist CRP-XL **and the GPCR
  agonist thrombin**, with both DTS store release and the subsequent SOCE phase
  reduced. Because Ghatge shows the reduction under thrombin — the same GPCR class
  this experiment drives — the contradiction with the model is direct.
- **Mechanism — impaired store-operated entry above all, not a simple buffer.**
  The one mechanism *both* papers support is reduced **store-operated Ca²⁺ entry**
  (Orai1/STIM1): each measured SOCE down. Beyond that they diverge, and both are
  explicit that the underlying cause is unresolved. Ghatge *speculates* that MCU
  loss also depletes the DTS store — via disrupted mitochondria–DTS
  (mitochondria-associated-membrane, MAM) coupling — and did see reduced
  thapsigargin-evoked store release. Ajanel, by contrast, *directly measured* DTS
  store content (ionomycin) and passive thapsigargin-evoked release and found
  **both normal**, so it attributes the defect not to a depleted store but to
  impaired Orai1/STIM1 gating, alongside mitochondrial bioenergetics and
  mitochondrial **ROS → Syk/PLCγ2 (ITAM)** signalling, and calls the link between
  MCU loss and lower cytosolic Ca²⁺ "unclear." (Ajanel also notes the older
  expectation — Brass, 1984 — that blocking mitochondrial uptake leaves cytosolic
  Ca²⁺ essentially **unchanged**, *not* elevated.) So store-*content* depletion is
  contested — seen in one study, excluded by the other — whereas reduced SOCE is
  the robust, shared finding. In neither account does MCU behave as a pure
  cytosolic buffer whose removal would raise free Ca²⁺.

(The cytosolic-Ca²⁺ findings are reported directionally — Ghatge as area-under-
the-curve of Fura-2 traces, Ajanel in supplemental figures; neither gives resting
or peak nM values, so there is no magnitude to fit against — but the *sign* is
unambiguous and consistent across both studies.)

## Honest comparison

- **Direction now matches.** With the v0.7 coupling, MCU loss *lowers* cytosolic
  Ca²⁺ and reduces agonist-evoked store release — the direction both studies
  report. The buffer-only model got the sign wrong; the coupling fixes it.
- **What the coupling represents.** A lumped matrix-Ca²⁺ support factor gating
  IP3R release (mitochondria–DTS / MAM arm) and SOCE (bioenergetic arm) — the two
  reductions *both* papers observe (reduced agonist-evoked release; reduced SOCE).
  The store-release arm provides the flip; SOCE alone is too weak (the extrusion
  pumps compensate).
- **Matched, not predicted.** This is honest model-building, not an independent
  prediction: we added the biology the papers point to and it reproduces their
  direction. The magnitude is a calibration choice (`E_ref` ≈ resting matrix);
  the papers report only directional/AUC data, so there is no magnitude to fit.
- **Still partial.** The model captures the cytosolic-Ca²⁺/release direction, not
  the full mechanism — mitochondrial **ROS → Syk/PLCγ2 (ITAM)** signalling and the
  GPVI pathway Ajanel emphasises remain out of single-cell GPCR scope. And the
  store-*content* question the papers disagree on (Ghatge depleted vs Ajanel
  normal) is not settled here; the model represents reduced *release*, not altered
  resting content (the KO store actually stays fuller because it releases less).
- **Dolan preserved.** The wild type is byte-identical (the support factor is 1
  whenever the matrix sits at its normal load), so the Dolan & Diamond 5/5
  validation is untouched; only MCU-perturbed runs change.

## Take-away

This is the most instructive result in the set: the **full modelling cycle in one
experiment**. A buffer-only MCU made a clear, mechanistically transparent
prediction; confronted with data it was wrong in *direction* (two MCU-knockout
studies — Ghatge 2026, Ajanel 2025 — both find cytosolic Ca²⁺ *reduced*); that
diagnostic divergence localised the missing biology (MCU's *support* of
agonist-evoked release and store-operated entry); and adding exactly that — the
#76 Part 1 capacity bound plus the Part 2 matrix-Ca²⁺ support coupling —
**resolved it**, so the model now reproduces the published direction. The honest
caveats (matched by adding the right biology rather than predicted; release-driven;
ROS/ITAM still absent; the store-content disagreement unsettled) are recorded
above. That arc — predict → confront → diagnose → extend → reproduce — is what a
mechanistic model is *for*. Tracked as GitHub issue #76.

## Provenance

The simulations were produced by `runscripts/manual/runFromConfig.py`, and the
figure by `runscripts/manual/analysisPlatelet.py --plot demo_calcium` (figure
module `models/platelet/analysis/single/demo_calcium.py`, reading the
`CalciumTrace` listener; the grey baseline overlay is supplied through the
`PLATELET_BASELINE_SIMOUT` environment variable). Both runs used **seed 0** for
**300 s** on Python 3.11.5. The figure and numbers were **regenerated for issue
#76 Part 1 + Part 2** (the matrix capacity bound plus the matrix-Ca²⁺ support
coupling that gates IP3R release and SOCE); the exact commit is stamped in the
figure footnote and in each run's `metadata/metadata.json`.

## Appendix — reproduce {#appendix}

From the repository root (`PYTHONPATH=$PWD`):

```bash
# Baseline run (provides the grey overlay)
cat > base.json <<'EOF'
{"length_sec": 300, "seed": 0, "run_config": {"agonist_delay_s": 60}}
EOF
python runscripts/manual/runFromConfig.py --config base.json --out exp_base

# MCU knockout: mitochondrial Ca2+ uptake removed
cat > mcu.json <<'EOF'
{"length_sec": 300, "seed": 0,
 "run_config": {"agonist_delay_s": 60, "mcu_vmax_scale": 0.0}}
EOF
python runscripts/manual/runFromConfig.py --config mcu.json --out exp_mcu

# Figure: MCU knockout (coloured) over the baseline (grey)
base=out/exp_base/platelet_stub_000000/000000/generation_000000/000000/simOut
PLATELET_BASELINE_SIMOUT=$base MPLBACKEND=Agg \
  python runscripts/manual/analysisPlatelet.py exp_mcu --plot demo_calcium
```

The figure module `demo_calcium.py` plots the `CalciumTrace` columns `ca_cyt_nM`,
`ca_dts_uM`, `soce_flux_nMs` and `ip3_nM`.
